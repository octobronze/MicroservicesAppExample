server:
  port: ${SERVER_PORT}

spring:
  application:
    name: user-service

eureka:
  instance:
    hostname:
      ${EUREKA_INSTANCE_HOSTNAME}
  client:
    service-url:
      defaultZone : ${EUREKA_URL}